



DO EVERYTHING BELOW STEP BY STEP AND ASK EVERY TIME BEFORE PASSING THE NEXT ONE!!!!!!!



1-i created an table named operator\_tablosu in DB check it first and undertand the structure and put at like 10 operators in it most important part is aktif at lest 3 of them should be false and also operator-rolu always should be dokumcu for every single one of them in the future there will be different rol but rn only dokumcu

2-there will be combobox right next to dokum zamanı input box, the combobox will show aktif operators

3-the user will be able the select aktif true operators for that dokum

4-and also in the postman we only should be able to POST aktif true operators aswell

5-aktif shows that if the operator still works or not

6-there is the catch actually so in the past dokums an opeator worked but maybe that opeartor is retired now so when we click on the detay gör button if the operators aktif false it gotta indicate it like "Bu operator artık çalışmamaktadır" or something like this

