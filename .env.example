# docker compose bu dosyanin .env kopyasini otomatik okur.
#   cp .env.example .env
# JWT secret HS256 icin en az 32 byte olmali:  openssl rand -base64 48
DB_USER=postgres
DB_PASSWORD=CHANGE_ME
JWT_SECRET=CHANGE_ME_AT_LEAST_32_BYTES_LONG
